# Shared Types

Reserved for shared API contracts between the cloud API, web dashboard, mobile
app, and edge agent.

Use this only after the API schema stabilizes. Until then, avoid introducing a
shared package that slows down iteration.
