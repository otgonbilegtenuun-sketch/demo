# Shared UI

Reserved for shared UI components once the Next.js dashboard exists.

Do not move the current edge-agent static dashboard here. The local dashboard is
still part of `apps/edge-agent` because it is served directly by the school edge
server.
