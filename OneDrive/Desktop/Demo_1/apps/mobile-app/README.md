# Mobile App

Reserved for a future mobile client.

Recommended options:

- React Native if the web dashboard is Next.js/React.
- Flutter if the team wants a separate mobile-first UI toolkit.

Build this only after the cloud dashboard and school-edge workflows are stable.
